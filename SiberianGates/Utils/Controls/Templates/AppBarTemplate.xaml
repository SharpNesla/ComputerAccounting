<UserControl x:Class="SiberianGates.Utils.Controls.Templates.AppBarTemplate"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SiberianGates.Utils.Controls.Templates"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Template>
        <ControlTemplate TargetType="{x:Type local:AppBarTemplate}">
            <DockPanel LastChildFill="True" Background="{DynamicResource MaterialDesignPaper}">
                <materialDesign:ColorZone DockPanel.Dock="Top" materialDesign:ShadowAssist.ShadowDepth="Depth1" Mode="PrimaryMid"
                                          Padding="16" Height="64">
                    <ContentPresenter ContentSource="HeaderTemplate"/>
                </materialDesign:ColorZone>
                <ContentPresenter/>
            </DockPanel>
        </ControlTemplate>
    </UserControl.Template>

</UserControl>
